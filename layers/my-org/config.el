;; -*- lexical-binding: t -*-

(defcustom org-inline-image-background "#FDF6E3"
  "The color used as the default background for inline images.
When nil, use the default face background."
  :group 'org
  :type '(choice color (const nil)))
